// Enables the two security rules the merge fixture's Feature trips, so the e2e tests can assert
// they fire only when -merge-features is given.
{
	"rules": {
		"no-privileged-container": "error",
		"no-docker-socket-mount": "error"
	}
}
